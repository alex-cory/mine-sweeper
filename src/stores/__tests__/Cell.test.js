// import game from '../GameStore'

describe('Cell Store Test', () => {
  it('should be either a number or a mine', () => {
      // expect(game.status).toMatch(/[💣|0-9]/g);
      expect(1 + 1).toEqual(2)
  });
});